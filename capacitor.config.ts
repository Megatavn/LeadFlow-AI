import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.vuhoang.leadflowai',
  appName: 'LeadFlow AI',
  webDir: 'dist',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#f7f2e9'
  }
}

export default config
